# app/hr-port (copied from dynamic-web-app) reads these. The API base URL is never
# reached — MSW intercepts every request against it.
VITE_APP_API_URL=https://hr-port.local/api
VITE_APP_ENABLE_API_MOCKING=true
VITE_APP_DEBUG_MODE=false
